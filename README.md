# Medical-Data-Extraction-Project
A Python-based Medical Data Extraction system that processes scanned prescriptions using OpenCV, regex, and custom Python classes. It extracts patient details, applies image thresholding, performs text parsing, and includes unit tests with pytest for reliable data extraction.
